import React from 'react';
import PropTypes from 'prop-types';
import CustomLogin from '../components/Forms/Login';

function Login(props) {
  return (
    <div>
      <CustomLogin />
    </div>
  );
}

Login.propTypes = {};

export default Login;
